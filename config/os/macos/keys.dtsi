/* Shared Keys */

// tap:      escape       
// long-tap: BAS layer
// shift:    caps lock
#define MKY_ESC  &mp_mk_ec

// tap: return
// long-tap: F2 (easy rename on ide)
#define MKY_RET  &mt  F2  RET
