TO_MACRO(m, DEV)  //to_mDEV
TO_MACRO(m, AXN)  //to_mAXN
TO_MACRO(m, FNK)  //to_mFNK
TO_MACRO(m, STG)  //to_mSTG

to_mBAS_kp: to_mBAS_kp {
    compatible = "zmk,behavior-hold-tap";
    #binding-cells = <2>;
    tapping-term-ms = <HP_TAPPING>;
    flavor = "hold-preferred";
    bindings = <&to_mBAS>, <&kp>;
};